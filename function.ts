let log = function(message){
    console.log(message);
}
let doLog = (message) => {
    console.log(message);
}//act same as above function => lamda shortcut to fun key
let doSomething = (message ) => console.log(message)  // for single line function